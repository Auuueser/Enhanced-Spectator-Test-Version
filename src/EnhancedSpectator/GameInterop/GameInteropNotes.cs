namespace EnhancedSpectator.GameInterop;

/// <summary>
/// Documents the current game interop boundary for code consumers.
/// </summary>
public static class GameInteropNotes
{
    /// <summary>
    /// Summary of the current interop policy.
    /// </summary>
    public const string Summary =
        "Game interop is adapter-only and uses confirmed Lethal Company members through direct publicized access.";
}
